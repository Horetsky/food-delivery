import {
    FoodList,
    ProductFilters
} from '../../modules'

const Home = () => {
    return (
        <div>
            <ProductFilters />
            <FoodList />
        </div>
    );
};

export default Home;